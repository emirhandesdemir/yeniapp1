
// This file is intentionally left blank to resolve the routing conflict
// between the root layout and the (main) layout group.
// The actual home page is now served by /src/app/(main)/page.tsx.

export default function RootPage() {
  return null;
}
